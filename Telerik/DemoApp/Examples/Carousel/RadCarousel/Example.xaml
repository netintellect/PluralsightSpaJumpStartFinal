<UserControl x:Class="Telerik.Windows.Examples.Carousel.RadCarousel.Example"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:examples="clr-namespace:Telerik.Windows.Examples"
			 xmlns:telerikQuickStart="clr-namespace:Telerik.Windows.Controls.QuickStart;assembly=QuickStart.Common"
			 xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation">
	
	<telerikQuickStart:QuickStart.ExampleHeader>
		<TextBlock Text="RadCarousel"
				   Margin="8" />
	</telerikQuickStart:QuickStart.ExampleHeader>
	
	<Grid>
		<telerik:RadCarousel x:Name="sampleRadCarousel"
							 ItemsSource="{Binding EmployeesCollection}"
							 AutoGenerateDataPresenters="False"
							 Background="Transparent">
			<telerik:RadCarousel.Resources>
				<telerik:BinaryImageConverter x:Key="converter" />
				<DataTemplate DataType="{x:Type examples:Employee}">
					<Image Source="{Binding Picture, Converter={StaticResource converter}}" />
				</DataTemplate>
			</telerik:RadCarousel.Resources>
		</telerik:RadCarousel>
	</Grid>
	<telerikQuickStart:QuickStart.ConfigurationPanel>
		<StackPanel Margin="3">
			<CheckBox Content="Show horizontal scrollbar"
					  IsChecked="True"
					  Margin="0,2"
					  Unchecked="HorizontalSrollBarVisibility_Unchecked"
					  Checked="HorizontalSrollBarVisibility_Checked" />
			<CheckBox Content="Show vertical scrollbar"
					  IsChecked="True"
					  Margin="0,2"
					  Unchecked="VerticalSrollBarVisibility_Unchecked"
					  Checked="VerticalSrollBarVisibility_Checked" />
			<StackPanel Margin="0,10,0,0">
				<TextBlock Text="Locate contact:" />
				<telerik:RadComboBox Margin="0,2"
									 DisplayMemberPath="FirstName"
									 ItemsSource="{Binding EmployeesCollection}"
									 HorizontalAlignment="Stretch"
									 ClipToBounds="True"
									 SelectionChanged="Contact_SelectionChanged" />
			</StackPanel>
		</StackPanel>
	</telerikQuickStart:QuickStart.ConfigurationPanel>
</UserControl>